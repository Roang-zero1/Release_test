name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

jobs:
  release:
    uses: Roang-zero1/workflows/.github/workflows/create-release.yml@v1
  update-major-version-tag:
    needs: ["release"]
    uses: Roang-zero1/workflows/.github/workflows/update-major-version-tag.yml@testing

